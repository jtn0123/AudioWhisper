import Foundation

// MARK: - Window Titles
// Centralized window title constants to avoid string duplication and silent failures

internal enum WindowTitles {
    static let recording = "AudioWhisper Recording"
}
